if (Ektron === undefined) {
    Ektron = {};
}
if (Ektron.PFWidgets === undefined) {
    Ektron.PFWidgets = {};
}
Ektron.PFWidgets.EktronTwitterFeed = {
    init: function (settings) {
        var container = $("#" + settings.ContainerID);
        
    }
};